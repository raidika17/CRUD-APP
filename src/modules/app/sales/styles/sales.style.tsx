import style from "./sales.module.css";

const SalesStyles = {
  LABEL: style.label,
  LABEL_DESC: style.label_desc,
  CATEGORIES: style.categories,
  AMOUNT: style.amount,
  ITEM_SOLD: style.item_sold,
  PRICE: style.price,
};

export default SalesStyles;
